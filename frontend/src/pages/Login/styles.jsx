import { styled } from "styled-components";

const SubmitButton = styled.button`
    background-color: #007bff;
    color: white;
    padding: 10px 20px;
    cursor: pointer;
`
export default {
    SubmitButton,
}